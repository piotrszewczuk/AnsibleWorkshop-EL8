---
- name: "Playbook 14 - Play1 - Vault"
  hosts: www
  vars_files:
    - tajne-dane.yaml
    - tajne-dane2.yaml
  tasks:
  - name: "Test pliku1"
    stat:
      path: '{{ zmienna_1 }}'
    register: plik1_test

  - name: "Utworz plik"
    file:
      path: '{{ zmienna_1 }}'
      state: touch
    when: plik1_test

  - name: "Test pliku2"
    stat:
      path: '{{ zmienna_2 }}'
    register: plik2_test

  - name: "Utworz plik"
    file:
      path: '{{ zmienna_2 }}'
      state: touch
    when: plik2_test
...
